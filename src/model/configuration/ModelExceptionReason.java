package model.configuration;

public enum ModelExceptionReason {
    INVALID_CONFIG_KEY,
    INVALID_CONFIG_FILE,
    FILE_NOT_FOUND,
    DIRECTORY,
    NOT_A_FILE,
    NOT_A_CSV_FILE,
    ENTITY_CLASS_NOT_FOUND
}
